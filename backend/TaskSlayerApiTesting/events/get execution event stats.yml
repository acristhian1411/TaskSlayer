info:
  name: get execution event stats
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/task-executions/{{execution_id}}/events/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
