info:
  name: delete event
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/task-events/{{task_event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
