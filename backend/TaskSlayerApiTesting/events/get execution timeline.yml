info:
  name: get execution timeline
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/task-executions/{{execution_id}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
