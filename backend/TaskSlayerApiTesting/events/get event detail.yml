info:
  name: get event detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/task-events/{{task_event_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
