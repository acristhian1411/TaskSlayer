info:
  name: get events
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/task-events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
