info:
  name: start execution
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/tasks/{{task_id}}/executions/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
