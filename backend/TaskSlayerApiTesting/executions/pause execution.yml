info:
  name: pause execution
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/task-executions/{{execution_id}}/pause"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
