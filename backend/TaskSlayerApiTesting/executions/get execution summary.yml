info:
  name: get execution summary
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/task-executions/metrics/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
