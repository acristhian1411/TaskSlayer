info:
  name: get time by task
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/task-executions/metrics/time-by-task"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
