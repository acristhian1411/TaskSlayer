info:
  name: resume execution
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/task-executions/{{execution_id}}/resume"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
