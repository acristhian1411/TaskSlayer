info:
  name: get my redemptions
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/reward-redemptions/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
