info:
  name: redeem reward
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/rewards/{{reward_id}}/redeem"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
