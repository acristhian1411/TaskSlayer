info:
  name: get redemption detail
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/reward-redemptions/me/{{redemption_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
