info:
  name: get points ledger
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/points/ledger"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
