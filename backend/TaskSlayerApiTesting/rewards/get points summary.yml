info:
  name: get points summary
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/points/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
