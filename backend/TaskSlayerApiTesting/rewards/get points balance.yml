info:
  name: get points balance
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/points/balance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
